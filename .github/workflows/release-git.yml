name: Release · Git

on:
  push:
    tags:
      - 'git/v*'

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_release-build.yml
    with:
      module: git
      module_path: modules/git
      tag: ${{ github.ref_name }}
      product_name: Git
    secrets: inherit
